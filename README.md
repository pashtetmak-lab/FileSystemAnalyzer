# FileSystemAnalyzer

## Описание проекта
FileSystemAnalyzer — консольное приложение на C++, которое сканирует файловую систему, строит дерево каталогов и файлов, вычисляет размеры директорий и выводит результат в читаемом виде.
##### Входные данные:
- путь к файлу или директории;
- опциональный аргумент `--depth N` для ограничения глубины обхода.

##### Результат работы:
- дерево файлов и директорий;
- размер файлов в байтах;
- суммарный размер директорий, посчитанный по дочерним узлам.

##### Входные данные:
- путь к файлу или директории;
- опциональный аргумент `--depth N` для ограничения глубины обхода.

##### Результат работы:
- дерево файлов и директорий;
- размер файлов в байтах;
- суммарный размер директорий, посчитанный по дочерним узлам.
### Пример работы

Ввод:

```bash
./cmake-build-release-gcc15/FileSystemAnalyzer . --depth 1
```

Вывод:

```text
./ (40844 B)
  CMakeLists.txt (1059 B)
  include/ (0 B)
  README.md (5094 B)
  .gitignore (639 B)
  cmake-build-release-gcc15/ (0 B)
  .git/ (0 B)
  main (33928 B)
  main.cpp (124 B)
  .idea/ (0 B)
  src/ (0 B)
```

## Структура проекта
Основные файлы и директории:
- `main.cpp` — точка входа в приложение;
- `include/` — заголовочные файлы классов;
- `src/` — реализации классов;
- `tests/` — тесты;
- `CMakeLists.txt` — конфигурация сборки CMake;
- `README.md` — описание проекта.

## Тесты
#### Сборка
```
cmake --build cmake-build-release-gcc15 --target unit_tests
```
#### Вызов
```
cmake-build-release-gcc15/unit_tests
```
#### Вывод
```
unit_tests passed
``` 
